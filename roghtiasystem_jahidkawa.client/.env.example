# ASP.NET API origin (private: never exposed to the browser).
NUXT_AUTH_API_BASE=http://localhost:5201
# Local HTTP development only. Use true for HTTPS production deployment.
NUXT_AUTH_COOKIE_SECURE=false
